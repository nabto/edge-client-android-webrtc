<resources>
    <!-- The UI is Compose; this theme only styles the window before setContent runs. -->
    <style name="Theme.EdgeClientAndroid" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
